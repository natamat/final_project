require_relative 'lib/sat-twilio.rb'
require_relative 'lib/sat-scrape.rb'

task :daily_question do 
  @send_message = Text.new(ENV[TO_NUMBER])
  @send_message.send

  #create a new instance of your twilio messaging class
  #call the send method on that instance
end

